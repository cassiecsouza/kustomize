pipeline:
  name: Templates
  identifier: Template
  projectIdentifier: Demo
  orgIdentifier: default
  tags: {}
  stages:
    - stage:
        name: Test Templates
        identifier: Test_Templates
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: harnessse
              namespace: cassie-dev
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          execution:
            steps:
              - step:
                  name: Account Level Template
                  identifier: Account_Level_Template
                  template:
                    templateRef: account.test
                    versionLabel: "0.1"
              - step:
                  name: Project Level
                  identifier: Build_preparation_step
                  template:
                    templateRef: Build_Preparation
                    versionLabel: "0.1"
  properties:
    ci:
      codebase:
        connectorRef: demo
        repoName: kustomize
        build: <+input>
